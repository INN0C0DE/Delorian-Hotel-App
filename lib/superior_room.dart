import 'package:flutter/material.dart';

class SuperiorRoomPage extends StatelessWidget {
  const SuperiorRoomPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Delorian Hotel: Superior Rooms'),
      ),
    );
  }
}
